#!/bin/bash

# Script to apply pywal colors to iTerm2
# This script creates a dynamic profile for iTerm2 using pywal colors

# Run the Python script to create the iTerm2 profile
python3 "$HOME/.config/wal/scripts/update_iterm2.py"
